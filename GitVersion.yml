mode: ContinuousDeployment
next-version: 1.1.0
branches:
  master:
    regex: ^master$
    increment: Patch
    label: ""
    is-main-branch: true
  dev:
    regex: ^dev[/-]
    increment: Minor
    label: dev
    source-branches:
      - master
